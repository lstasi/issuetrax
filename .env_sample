# GitHub Personal Access Token
# Generate one at: https://github.com/settings/tokens
# Required scopes: repo, read:user
GITHUB_TOKEN=ghp_your_token_here

# Test Repository (optional - defaults to lstasi/issuetrax)
TEST_REPO_OWNER=lstasi
TEST_REPO_NAME=issuetrax

# Test PR Number (optional - defaults to 1)
TEST_PR_NUMBER=1
